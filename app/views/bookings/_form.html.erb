<%= simple_form_for [@property, @booking], html: { class: "edit_form"} do |f| %>
  <%= f.input :booking_start, as: :datetime, html5: true %>
  <%= f.input :booking_end, as: :datetime, html5: true %>
  <%= f.submit "Reserve", class: "btn-search btn-search-index" %>
<% end %>
